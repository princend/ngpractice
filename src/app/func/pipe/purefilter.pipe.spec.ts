import { PurefilterPipe } from './purefilter.pipe';

describe('PurefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PurefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
