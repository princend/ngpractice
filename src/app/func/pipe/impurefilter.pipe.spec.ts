import { ImpurefilterPipe } from './impurefilter.pipe';

describe('ImpurefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpurefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
